//Programa7.2
//Laura Portillo
//24 de octubre del 2025
//Funcionamiento de  las condiciones de if
//-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

#include<stdio.h>
int  main()
{
 if(0)// es nuestra condición
  {
   printf("Esta instruccion nunca se ejecuta \n.");
   printf("porque la condición siempre es falsa (0). \n");
  }
 if(-38)// Cuando ponemos un numero  diferente a certo se ejecuta el programa
   printf("Esta instruicción siempre se ejecuta. \n");
  return 0;
}
